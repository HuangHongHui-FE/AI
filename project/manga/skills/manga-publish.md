# 漫画发文 Skill（主流程）

> 项目根目录：`/Users/zcy1/code_self/AI/project/manga`
> 调用方式：用户说「写漫画」「发一篇漫画」「来一波漫画」之类，Claude **自动**选点、配文、排版、推草稿——写完自查 preflight 硬门与漫画原创合规
> 推草稿脚本细节见 `src/publish.js`，本文写**选点 + 双线配文 + 原创红线 + 串流程**

---

## 触发条件

用户说「写漫画」「发一篇漫画」「来一波漫画」「写个 XX 主题的漫画」之类。

**执行原则**：触发后**不再问澄清问题**，直接跑完整流程——选点 → 配文（A/B 线随机）→ 写 article.json → preflight 自查 → 推草稿 → 写日志。中途模糊判断（走 A 还是 B 线、标题怎么起、收口选哪类、CTA 选哪句）自己定。仅当真正阻塞（.env 没配、comics/ 没图、preflight 反复 FAIL）才停下来报告。

---

## 依赖与首次配置

- **推草稿脚本**：本项目 `node src/publish.js`（已写好）。`src/wechat.js`+`src/config.js` 从 wechat-img 零改拷贝，token 缓存隔离到 `manga/cache/token.json`，**不串 wechat-img 的号**。`src/publish.js` 复用 `../wechat-img/src/html.js`（markdown→HTML + 主题池，无状态，跨项目 import）。
- **画图脚本（默认·线条角色）**：本项目 `node src/draw.js`（**程序化 SVG 线条漫画，零 AI 零 key，原创绘制可勾漫画原创**）。用 sharp 把 SVG 渲染成 jpg。角色是**有辨识度的线条小人**（圆脸五官 + 固定发型/配饰/服饰，见「线条角色构件表」），表情 `emo` + 动作 `pose` 控制，靠角色固定特征跨格/跨篇辨人。Claude 写「舞台调度」到 `panels`，draw.js 解释渲染，**不碰 SVG**。（已废弃 AI 生图线 `src/gen.js`：出图不可控/爱出风景/文字乱码，不用于生产。）
- **漫画图**：由 `draw.js` 按 `article.json` 的 `panels` 字段生成到 `comics/<slug>/01.jpg..NN.jpg`（按 panels 顺序）。Claude 不用手画、不用找图。
- **首次配置 `.env`**（本项目根目录，漫画号独立 appid/secret，别和 wechat-img/mingchangmian 混用）：
  ```
  WECHAT_APPID=漫画号的appid
  WECHAT_APPSECRET=漫画号的secret
  AUTHOR_NAME=漫画号署名
  ```
- **目录**：`comics/`（原图）、`output/YYYY-MM-DD/NN-slug/`（产物）、`logs/YYYY-MM-DD.md`（日志）、`cache/`（token）跑时自动建。

---

## 选点 / 选题

### 1. 选题来源（按优先级）

a. **用户指定**：用户点名某主题（「写个职场内卷的漫画」「写猫狗日常」），直接用。
b. **蹭当下热点**：若有可漫画化的社会热点（职场/婚恋/房价/教育/养老），优先蹭——漫画号流量大头。用 WebSearch 探近 3 天热点，挑能画成连续分镜的。
c. **自选未写过的**：从下列池挑一条 `logs/` 没写过的（非穷尽）：
   - **生活治愈**：独居日常 / 宠物 / 一人食 / 通勤 / 周末发呆
   - **职场毒打**：内卷 / 加班 / 画饼 / 同事 / 跳槽 / 年底述职
   - **情感共鸣**：相亲 / 催婚 / 分手 / 友情 / 异地
   - **人间百态**：买房烂尾 / 养老 / 鸡娃 / 中年危机 / 数字游民
   - **纯分镜搞笑**：反转 / 谐音 / 日常尴尬（适合 A 线少字）

### 2. 去重

扫本项目 `logs/*.md` 的「主题」列，候选若和历史某篇同主题（语义同即算），换一条。

```bash
grep -hE '^\| 0[0-9]' logs/*.md | awk -F'|' '{gsub(/^ +| +$/,"",$4); print $4}' | sort -u
```

### 3. 篇数

默认 **1 篇**（用户喊「来一波」「来几篇」才多写）。每篇独立选点、独立配文、独立推。

---

## 连续剧模式（老周家，默认固定）

用户漫画**围绕「家庭生活」单一题材，固定主角团「老周家」，连续剧式连载**（每篇是一集，人物/世界观贯穿）。这是默认创作模式，不再随机选题材。

### 主角团与角色卡（`characters.json`）

项目根 `characters.json` 定义主角团固定身份/性格（**与 `draw.js` 角色构件一一对应**）：

| 角色 | 身份 | 构件 type | 线条特征（画稿固定） |
|---|---|---|---|
| **周建国** | 爸爸·程序员 40岁 | `zhoujianguo` | 发际线略后移黑发、黑框眼镜、浅蓝格子衫 |
| **林晓** | 妈妈·小学老师 35岁 | `linxiao` | 短发、细框眼镜、粉色针织衫 |
| **周小满** | 女儿·小学三年级 9岁 | `zhouxiaoman` | 两个小辫子、红裙子 |
| **周爷爷** | 爷爷·退休工人 68岁 | `zhouyeye` | 白发、老花镜、白背心 |
| **王阿姨** | 对门邻居 50岁 | `wangayi` | 烫卷发、碎花上衣 |

- **`draw.js` 每个角色一个构件函数**：`panel.actors` 里写 `type:"zhouyeye"` 即画周爷爷（圆脸五官 + 固定发型/配饰/服饰），特征跨格固定 → 辨识度高、不用每格重复描述。
- **换角色/改外貌**：改 `src/draw.js` 对应角色构件的 `hair/shirt/extra`，全项目生效。加新角色在 `src/draw.js` 加构件函数 + `ACTORS` 注册 + `characters.json` 加项。

### 世界观（固定）

> 一座普通二线城市的老小区三居室，老周一家四口和爷爷同住，楼下有菜市场，小区门口有小学，对门住着爱串门的王阿姨。

所有集都发生在这个世界，场景/人物关系稳定。

### 连续剧写法

- **标题带集数**：`老周家·第N集：<本集主题>`（如「老周家·第1集：期末考前一晚」）。标题骨架池的文案可用作副标题。
- **每篇仍独立成篇**：单集也有起承转合（B 线 300-1500 字/ A 线少字都适用），新读者单看一集也能看懂，不硬性依赖前情。
- **logs 记剧情进度**：写日志时备注栏记「剧情：本集<发生的事>，埋<伏笔>」，下集避开情节矛盾、可接续伏笔。
- **取材方向**（家庭生活，非穷尽）：鸡娃（辅导作业/兴趣班/升学）、养老（爷爷智能手机/保健品/看病）、代际（婆媳/隔代亲/观念冲突）、夫妻（中年压力/互相体谅）、日常治愈（周末/年夜饭/搬家）。每集换一个切口，避开历史日志。
- **角色出场不必每集全员**：按本集情节需要带 2-4 个角色即可，不硬塞全体。

---

## 两条文字线（随机轮换，避同质化）

参考两篇范例：
- **A 线·少字看图**（仿《【漫画】在游戏厅认识的性感大姐姐》：一段引子 → 连续 N 张漫画图 → 结束，~100 字）
- **B 线·长文叙事**（仿《治愈漫画|你若爱，生活处处皆可爱》：开头钩子 → 漫画图按情节穿插 → 中段转折 → 治愈/犀利收口 → CTA，~1300 字）

### 选线规则

- 看漫画素材：**分镜强、图自带笑点/泪点** → A 线；**有故事弧、需观点增量** → B 线。
- 扫近 5 篇日志，**避免连发同线**（连发 3 篇 A 则强制 B，反之亦然）。
- 把 `article.line` 字段写成 `"A"` 或 `"B"`，preflight 按对应字数门判。

### A 线结构（少字看图，10-200 字）

```
一句引子（作品名/角色/一句钩子）
![第1格](comics/<slug>/01.jpg)
![第2格](comics/<slug>/02.jpg)
...
![第N格](comics/<slug>/NN.jpg)
```

引子写法（仿 wx1「今天推荐的是XX老师的作品《XX》，<钩子>」）：点出作品/角色 + 一句让人想往下看的话。不写分析、不写感悟——A 线靠图说话。

### B 线结构（长文叙事，300-1500 字）

```
开头钩子（一句戳痛点/共鸣点，不套「在...的今天」）
![第1格]...
<段1：场景/铺陈，承接第1格>
![第2格]...
<段2：推进/反转>
...
<中段转折：从毒打切到治愈，或从现象切到观点>
<收口金句（收口池抽一条）>
<CTA（CTA池抽一句）>
```

B 线要求（仿 wx2）：
- **图为叙事骨架，文为血肉**：每段文字承接前一张图、引出后一张图，不是看图说话、不是图配解说词。
- **有转折**：前半毒打/铺垫，后半治愈/犀利收口，别一路平铺。
- **有第一人称落点**：至少一处「我/我们/我朋友」（preflight 会查）。
- **有观点增量**：不止复述图里故事，要有自己的判断/类比/追问。

---

## 配图与排版

### 漫画图（draw.js 线条角色，程序化渲染）

Claude 在 `article.json` 写 `panels` 字段（声明式分镜，**只写舞台调度，不写 SVG**），`draw.js` 解释渲染成 `comics/<slug>/01.jpg..NN.jpg`：

```jsonc
"panels": [
  {
    "bg1": "#FFE9D6", "bg2": "#FFB677", "label": "01·画饼",  // 渐变底色 + 角标
    "actors": [{ "type": "zhoujianguo", "x": 150, "hipY": 430, "emo": "sad", "pose": "point" }],
    "props":  [{ "type": "pie", "cx": 340, "cy": 300, "r": 90 }],
    "bubbles":[{ "x": 250, "y": 110, "w": 200, "h": 90, "text": "三年买房", "px": 320, "py": 200 }]
  }
]
```

**线条角色构件表**（`type` 值，`draw.js` 每个角色一个构件，特征固定）：

| 角色 | type | 线条特征 | emo 表情 | pose 动作 |
|---|---|---|---|---|
| 周建国 | `zhoujianguo` | 发际线后移黑发+黑框眼镜+浅蓝格子衫 | happy/sad/angry/surprised/calm | down/up/point/shrug/hug/phone |
| 林晓 | `linxiao` | 短发+细框眼镜+粉针织衫 | 同上 | 同上 |
| 周小满 | `zhouxiaoman` | 双辫+红裙 | 同上 | 同上 |
| 周爷爷 | `zhouyeye` | 白发+老花镜+白背心 | 同上 | 同上 |
| 王阿姨 | `wangayi` | 烫卷发+碎花上衣 | 同上 | 同上 |

**道具构件**（`type` 值）：`pie`/`moon`/`desk`/`question`/`heart`/`bulb`/`rain`/`star`（参数见 `src/draw.js`）。

- **`emo` 表情**：happy 笑 / sad 愁 / angry 怒 / surprised 惊 / calm 平静（默认 calm）。靠表情+动作讲故事，**纯漫画可全格不写气泡**，靠画面让读者看懂剧情。
- **`pose` 动作**：down 垂手 / up 举手 / point 指 / shrug 耸肩 / hug 抱 / phone 举手机。
- 画布固定 480×640 竖图（手机条漫）。`x` 横坐标 0-480，`hipY` 角色「腰」（身体底）纵坐标，常 380-460。
- **分镜顺序 = panels 数组顺序 = body_markdown 里 `![]()` 顺序**，三者对齐：panels 写几格，draw 出几张，body_markdown 引用 `comics/<slug>/01.jpg..NN.jpg`。
- **图数 ≥3 才算漫画**（preflight WARN：<3 提示像单图插画）。建议 4-12 格。
- 构件不够用：在 `src/draw.js` 的 `PROPS`/`ACTORS` 加新构件函数，Claude 即可在 panels 引用。

### 封面

- `--image` 指封面图，通常用 `comics/<slug>/01.jpg`（第一格）或专门封面图。
- 默认**裸图封面**（不套 slogan，保漫画感）。要套 slogan 标语，后续可复用 `../wechat-img/src/cover.js`（暂未接入，手动跑或后续加）。

### 排版主题

- `article.theme` 可选；不填则 `pickRandomThemeName` 随机抽（复用热点号主题池，避同质化）。
- 漫画正文宜素净（少加粗、少引言块），preflight 会 WARN 加粗 >8。

### 摘要 digest

- ≤54 字，无换行无外链。A 线可写「作品名 + 一句钩子」，B 线写收口金句或核心共鸣点。

---

## ⭐ 原创红线（漫画原创声明合规，核心一节）

### 关键事实

微信 `draft/add` API 的 `articles[]` payload **不含原创声明字段**——漫画原创是**后台运营操作 + 发布时人手勾选**，不是发文接口能调的：

1. **申请漫画类目**：mp.weixin.qq.com 后台 → 原创声明 → 申请开通「漫画」类目（前提：账号已开原创声明功能）。
2. **发布时勾选**：草稿箱发布界面勾「原创声明」并选「漫画」类目。`publish.js` 推完草稿会提示「★ 此篇可勾漫画原创」。

### 可申漫画原创的硬条件

- ✅ **图为作者原创**：本人手绘/板绘，**或本项目 `draw.js` 程序化线条漫画原创绘制**（用工具原创生成都算，非搬运、非他人作品、非真人照片拼贴）。`draw.js` 是程序化 SVG 原创，**可勾漫画原创**。
- ✅ **连续画面构成有情节/有叙事的漫画**：非单张插画、非纯表情包、非无意义图堆砌。
- ✅ **文章主体是漫画图**：图为主、文为辅（B 线长文也不能图沦为插图，须图为叙事骨架）。
- ✅ **分镜/台词/场景有创作性投入**：非机械套同一模板批量造（避免低价值 AIGC 判定，对照 [[wechat-low-creation-spec]]）。

### 不可申漫画原创

- 🚫 **搬运他人漫画**（如日漫介绍式）：只能常规发表，**别勾原创**——勾了判侵权/洗稿，可能封原创功能。`article.original_eligible` 设 `false`，`publish.js` 会 WARN 提醒不勾原创。
- 🚫 纯文字无漫画 / 摄影 / 表情包堆图 / 单张插画。

### 申请话术模板（申请漫画类目时填）

```
本账号「<AUTHOR_NAME>」定位为原创漫画号，连载本人独立手绘/板绘的连续叙事漫画。
作品均为本人原创绘制，每篇由 4-15 格连续分镜构成完整故事情节，
文章主体为漫画画面，配少量文字旁白。
现申请开通「漫画」原创声明类目，承诺所发布漫画均为本人原创，无搬运洗稿。
代表作品：《<已发漫画标题1>》《<已发漫画标题2>》
```

---

## 自查 / preflight 硬门

推草稿前 `publish.js` 内嵌跑 `src/preflight.js`，grep/正则程序化判定，FAIL 则 exit 1 拒推（物理跳不过）。门：

| 门 | 规则 |
|---|---|
| 标题 | ≤22字；不得「我X了」情绪尾（看懵了/愣了下/笑了等）；无标题党词（震惊/速看/刚刚/突发/曝光） |
| digest | ≤54字；无换行；无外链 |
| 正文 | 无 ASCII 双引号「"」（改「」）；无正文超链接；无存疑词（据传/疑似/据说等）；无禁用八股词（综上所述/在当今社会/众所周知等） |
| 字数 | A 线 10-200 / B 线 300-1500；超范围 FAIL |
| 图数 | <3 张 WARN（像单图插画非漫画） |
| 原创 | `original_eligible=false` → WARN 不可勾漫画原创 |

删了热点号的「六段全套三件套」「AI 金句句式」门——漫画不套六段、A 线少字不适用金句门。

---

## 推草稿命令（串流程）

```bash
# 1. 画图：draw.js 读 panels 渲染 comics/<slug>/01..NN.jpg（线条角色）
node src/draw.js --from-json output/$(date +%F)/01-<slug>/article.json --slug <slug>

# 2. dry-run：只产预览 HTML，不调微信 API（先看效果）
node src/publish.js --slug <slug> --from-json output/$(date +%F)/01-<slug>/article.json \
  --image comics/<slug>/01.jpg --dry-run

# 3. 真推
node src/publish.js --slug <slug> --from-json output/$(date +%F)/01-<slug>/article.json \
  --image comics/<slug>/01.jpg
```

选项：`--author "<name>"` 覆盖署名；`--embed-image` 正文头部嵌首图（默认不嵌，漫画首格即首图避免重复）；`--out-dir <path>` 自定义输出目录。

---

## 写日志

推完后在 `logs/YYYY-MM-DD.md` 记一行（供下批避撞）：

```markdown
| 编号 | 主题 | 标题 | 线 | 字数 | 图数 | 主题名 | 收口类型 | CTA类型 | 原创可勾 | 备注 |
|---|---|---|---|---|---|---|---|---|---|---|
| 01 | 职场内卷 | 画饼老板的十二时辰 | B | 980 | 8 | cool | 治愈金句 | 转发引导 | 是 | 蹭年底述职热点 |
```

下批选点/选线/选收口/选CTA 前 grep 本表避撞。

---

## 已知坑

1. **原创勾选只能人手点**：`publish.js` 推草稿后不会自动勾原创，必须登录 mp.weixin.qq.com 草稿箱发布时勾。API 不支持。`draw.js` 程序化线条原创**可勾漫画原创**（发布时人手勾「原创声明→漫画」类目）。
2. **多图顺序上传**：`publish.js` 按 `body_markdown` 里 `![]()` 出现顺序逐张上传，顺序由你在 article.json 里排。某张失败会 WARN 跳过（微信正文留空图），推前看 dry-run 预览确认。
3. **封面首图选哪张**：默认第一格当封面。若第一格不适合当封面（如纯过场），指定一张专门的封面图给 `--image`。
4. **token 缓存隔离**：`manga/cache/token.json` 独立，别删 wechat-img 的 cache 误伤。
5. **A 线字数下限 10**：太短（<10）会被判信息量不足，至少一句引子。
6. **角色构件调整**：想改某个角色的长相/配饰，在 `src/draw.js` 对应构件函数的 `hair/shirt/extra` 改，全项目生效。

---

## 多维轮换池（避同质化，每篇从各池随机抽，记日志供下批避撞）

### 标题池 · 20 骨架（贴作品名/角色/场景词）

| # | 骨架 | 示例 |
|---|---|---|
| 1 | 分类前缀+主题 | 【漫画】在游戏厅认识的性感大姐姐 |
| 2 | 治愈系分类 | 治愈漫画\|你若爱，生活处处皆可爱 |
| 3 | 一句话钩子 | 画饼老板的十二时辰，句句扎心 |
| 4 | 原话引用 | 老板说「你是姓张吧」，我愣在原地 |
| 5 | 反差对照 | 月薪三千的我，操着三亿的心 |
| 6 | 设问 | 努力工作有什么意义，画给你看 |
| 7 | 进展式 | 又被催婚了，这次画成了漫画 |
| 8 | 数字前置 | 30岁还没结婚，错了吗 |
| 9 | 场景定格 | 期房交房那天，我看见烂尾楼 |
| 10 | 自嘲式 | 我把社畜的一天画了下来 |
| 11 | 共鸣式 | 这就是当代打工人的下班路 |
| 12 | 季节/时间 | 毕业五年后，我们成了这样 |
| 13 | 对比式 | 理想和现实，差了整本漫画 |
| 14 | 第二人称 | 你也曾这样等过一个人的消息 |
| 15 | 物拟人 | 我家猫眼里的我，是个废物 |
| 16 | 反问 | 谁说成年人不会哭，只是不敢 |
| 17 | 画面感 | 凌晨三点的写字楼，灯还亮着 |
| 18 | 治愈金句前移 | 愿你被世界温柔以待，漫画为证 |
| 19 | 毒打金句前移 | 生活给你挖坑，你得学会卖萌 |
| 20 | 留白式 | 这场戏，看懂的人不说话 |

### 收口池 · 20（B 线末尾金句，治愈/犀利轮换）

| # | 收口 |
|---|---|
| 1 | 你若爱，生活处处皆可爱。 |
| 2 | 岁月可以是杀猪刀，也可以是雕刻刀。 |
| 3 | 枯井里也能仰望繁星。 |
| 4 | 愿你向下扎根，向上开花，奔赴自己的星辰大海。 |
| 5 | 每一天，都是你余生最年轻的一天。 |
| 6 | 生活若不为热爱奔走，心态就被碾皱。 |
| 7 | 我们都在等生活给答案，生活却在等我们先开口。 |
| 8 | 成年人的崩溃，都是静音的。 |
| 9 | 不是生活太苦，是我们记得太清。 |
| 10 | 愿你心怀热爱，所遇皆温柔。 |
| 11 | 那个鲜活的自己，别弄丢了。 |
| 12 | 热爱生活的人，把琐碎过成短诗。 |
| 13 | 你满怀戾气，处处风声鹤唳；你满怀热爱，处处皆可爱。 |
| 14 | 最好的黄金时代，就是现在。 |
| 15 | 别等老了才想起，自己也曾鲜衣怒马。 |
| 16 | 生活挖的坑，就当是种花的花盆。 |
| 17 | 愿你历尽千帆，归来仍有少年气。 |
| 18 | 不尽如人意，才是人间真实。 |
| 19 | 熬过去的夜，都会变成光。 |
| 20 | 我们都在用不喜欢的方式，养活喜欢的自己。 |

### CTA 池 · 20（引导点赞关注转发，轮换）

| # | CTA |
|---|---|
| 1 | 觉得漫画有趣，别忘了点赞、关注、转发一条龙，后期还有更多精彩~ |
| 2 | 画得用心，看得开心的话，点个关注不迷路。 |
| 3 | 你的每个赞，都是我画下一格的动力。 |
| 4 | 感同身受的话，转给那个和你一样的人看看。 |
| 5 | 关注我，陪你画完每一个普通人的故事。 |
| 6 | 看懂了就点个「在看」，让更多人看到自己。 |
| 7 | 喜欢这种风格，留个言告诉我，下期继续。 |
| 8 | 转发是最大的鼓励，关注是最长的陪伴。 |
| 9 | 评论区聊聊，你也有过这样的瞬间吗？ |
| 10 | 画里有你，就点个关注，咱们慢慢处。 |
| 11 | 三连一下，让漫画被更多人看见。 |
| 12 | 觉得戳心，就转发给懂的人。 |
| 13 | 关注不走丢，下次更新第一时间通知你。 |
| 14 | 点赞是认可，转发是共鸣，关注是约定。 |
| 15 | 看完笑了或哭了，都欢迎来说一声。 |
| 16 | 每一格都是手画，喜欢就点个赞再走。 |
| 17 | 你的关注，是这个号画下去的理由。 |
| 18 | 戳「在看」，把这份治愈传给更多人。 |
| 19 | 留言区等你，说说你的版本。 |
| 20 | 漫画会继续，关注会值得。 |

### 结构配方池 · 20（B 线叙事弧轮换，避免篇篇同骨架）

| # | 配方 |
|---|---|
| 1 | 毒打铺陈→治愈转折→金句收口（wx2 原型） |
| 2 | 现象→追问→自嘲解构 |
| 3 | 过去自己 vs 现在自己 对照 |
| 4 | 一天时间线（早→午→晚） |
| 5 | 他/她视角讲一个普通人 |
| 6 | 设问开篇→逐格拆解→反问收口 |
| 7 | 集体群像（同事/家人/陌生人） |
| 8 | 物的视角（猫/房/通勤卡） |
| 9 | 季节/年龄轴线（二十→三十→四十） |
| 10 | 单一场景深挖（一顿饭/一次通勤） |
| 11 | 反转结构（先抑后扬/先扬后抑） |
| 12 | 清单体（打工人的 N 个瞬间） |
| 13 | 对话体（你问我答） |
| 14 | 假设式（如果当年……） |
| 15 | 对比式（理想格 vs 现实格交替） |
| 16 | 成长线（不会→会→看透） |
| 17 | 关系线（陌生人→熟人→走散） |
| 18 | 情绪曲线（平→升→崩→平复） |
| 19 | 谐音/双关贯穿 |
| 20 | 留白式（图多字少收口，A/B 混血） |

---

## 串流程速查

```
用户「写漫画」
 → 选点（连续剧默认老周家，看上集剧情避开同切口）
 → 设计分镜：想几格、每格谁在做什么/什么表情动作/什么场景道具 → 定 A/B 线（分镜强→A少字，有弧→B长文）
 → 写 article.json：title/digest/panels(每格 actors:角色type+emo表情+pose动作 + props/bubbles)/body_markdown(含 comics/<slug>/NN.jpg 多图)/line/cta_question/cta_follow/original_eligible(draw.js→true)
 → 标题/收口/CTA/结构配方 从池随机抽，记日志避撞
 → 画图：node src/draw.js --from-json ... --slug <slug>  → comics/<slug>/01..NN.jpg
 → dry-run 看预览：node src/publish.js --slug <slug> --from-json ... --image comics/<slug>/01.jpg --dry-run
   （图不对？改 actors/props 重跑 draw；文不对？改 body_markdown 重跑 publish）
 → 真推：去掉 --dry-run
 → 写 logs/YYYY-MM-DD.md 一行
 → 提醒用户：登录 mp.weixin.qq.com 草稿箱发布，原创可勾则勾「漫画」类目
```
