# 微信公众号贴图发布 Skill

> 项目根目录：`/Users/zcy1/code_self/AI/project/dy-chat-img`
> 调用方式：用户说「发贴图」「发图片消息」「发小绿书」等，或发来抖音链接
> 发布类型：微信公众号图片消息（贴图 / 小绿书），对应 `article_type: "newspic"`
> **默认数量：一次写 5 篇不同热点贴图**（2026-07-18 约定）。用户说「写贴图/继续写/多写几篇」时，默认抓当日微博热搜 Top、跳过已发话题、连写 5 篇，不再逐篇确认。每篇独立 JSON（`贴图草稿-YYYYMMDD-{a,b,c,d,e}.json`）+ 独立图素材，串行发布（同 AppID token 互踩，禁止并发）。

---

## 触发条件

用户表达想要发布**图片消息 / 贴图 / 小绿书**类型到微信公众号草稿箱，或发来抖音链接要求生成贴图，例如：

- 「发贴图」
- 「发图片消息」
- 「发小绿书」
- 「今天发一篇贴图」
- 「这是抖音链接，帮我发成贴图」
- 用户直接发来 `https://v.douyin.com/xxxxx/` 类链接

---

## 执行流程

触发后按以下顺序执行，**不再问澄清问题**，中途模糊判断（标题、配图、文案风格）自己定：

### 情况一：用户提供抖音链接

#### 1. 下载抖音原图

运行 `download_douyin.py`：

```bash
python3 download_douyin.py --url "https://v.douyin.com/xxxxx/" --out douyin_raw_imgs
```

脚本依赖 Playwright + Chromium，会：
- 用无头浏览器打开抖音页面
- 移除登录弹窗和遮罩
- 拦截页面图片的网络响应
- 把高清原图保存到 `douyin_raw_imgs/`

#### 2. WebP 转 JPEG

抖音原图可能是 webp 格式，微信 `material/add_material` 不直接支持，需要转换：

```bash
mkdir -p douyin_raw_imgs_jpg
for f in douyin_raw_imgs/*.webp; do
  sips -s format jpeg "$f" --out "douyin_raw_imgs_jpg/$(basename "$f" .webp).jpg"
done
```

#### 3. 剔除 UI/emoji 缩略图

下载的图里混有 UI 图标、头像、emoji 动图。按**像素尺寸**精确剔除：

```bash
for f in *.jpg; do
  w=$(sips -g pixelWidth "$f" | awk '/pixelWidth/{print $2}')
  h=$(sips -g pixelHeight "$f" | awk '/pixelHeight/{print $2}')
  echo "$f ${w}x${h}"
done | sort -k2 -n
```

剔除规则：宽或高 <300px 的小图（73x73、100x100、144x144 等 emoji）、细长条（如 774x92 横幅、128x7680 装饰条）、明显是头像/图标的图。保留真正正文图。

#### 4. OCR 识别 + 主题分析 + 撰写高互动配文

图片内容不限于聊天记录，可能是漫画、截图、海报等。先识别图中文字，再归纳主题：

```bash
# macOS 原生 Vision OCR，逐张提取文字（脚本已存于项目根 ocr.swift）
swift ocr.swift img_xxx.jpg img_yyy.jpg ...
```

OCR 若返回「无文字」（纯插画/漫画），改用**视觉读图**（Read 工具）理解画面内容。

综合所有图文字/画面，**归纳出一个主题**，再据此生成 `贴图草稿-YYYYMMDD.json`：

- `title`：贴合主题、有钩子、≤22 字，能让人忍不住点开
- `summary`：一句话总起反思，≤54 字（同时作为 digest）
- `cover.placeholder`：封面图片本地文件名（选最有冲击力的一张）
- `images`：正文图片列表，每项 `caption`（小标题）+ `text`（配文）

**文案核心原则——反思总结，不逐图描述：**

- 文案**不是一张张图分析**（不要写「这张图是 XX 期，图中显示……」），而是把整组图当作一篇连贯感悟，**从图片内容里反思总结出核心观点**。
- 每张图的 `text` **只写一句核心要点**（约 20-30 字），不展开成长段。`caption` 是这一句要点的反思角度（如「反复无常，不是性格」「讲道理最没用」）。
- 全篇按反思逻辑递进（如：破除误解 → 还原真相 → 给出方法 → 收束互动），读起来像一篇短文章，不是图解说明。
- 口语化、有情绪、不 AI 味，**目标是激发互动**：
  - 留钩子让人想评论（抛出争议点 / 替读者说出心里话 / 留一个未解的问号）
  - 适度「道德绑架」式引导（「不转不是姐妹」「看懂的都是过来人」「不转的下次别问为什么」之类，轻量、不惹反感）
  - 结尾 `ending` 直接引导点赞/评论/关注，给个互动理由

#### 5. 复制图片到项目根目录

```bash
cp douyin_raw_imgs_jpg/img_xxx.jpg cover-YYYYMMDD.jpg
cp douyin_raw_imgs_jpg/img_yyy.jpg img1.jpg
cp douyin_raw_imgs_jpg/img_zzz.jpg img2.jpg
# ...
```

**图片保持原始比例，不裁剪。** 微信图片消息会自适应显示，强行裁成统一比例反而会切掉正文内容（如长条聊天记录、竖图关键区域）。直接用转码后的原图即可。

封面 `cover-YYYYMMDD.jpg` 可与正文第一张相同，选冲击力最强的一张。

#### 6. 更新 Access Token

打开 `post_draft.js`，检查顶部 `ACCESS_TOKEN` 是否有效。`access_token` 有效期 2 小时，过期后重新获取并替换。

获取方式：

```bash
curl -s "https://api.weixin.qq.com/cgi-bin/token?grant_type=client_credential&appid=APPID&secret=APPSECRET"
```

#### 7. 发布到草稿箱

```bash
cd /Users/zcy1/code_self/AI/project/dy-chat-img
node post_draft.js --draft "贴图草稿-YYYYMMDD.json"
```

脚本会自动：

1. 读取草稿 JSON
2. 上传封面和正文图为微信永久素材
3. 按 `article_type: "newspic"` 调用 `draft/add` 创建图片消息草稿
4. 把返回的 `media_id` 写入 `draft_response.json`

#### 8. 记录发布结果

更新 `发布结果.md`，记录：

- 操作日期
- 来源抖音链接
- 草稿 `media_id`
- 标题
- 类型说明「图片消息（newspic）」
- 本次上传的素材 `media_id`

### 情况二：用户没有给链接，只给主题

#### 1. 生成贴图草稿 JSON

按规范生成文件 `贴图草稿-YYYYMMDD.json`：

- `title`：贴图标题，≤22 字
- `summary`：一句话总起反思，≤54 字（会同时作为 digest）
- `cover.placeholder`：封面图片本地文件名
- `images`：正文图片列表，每项包含 `caption`（小标题）和 `text`（配文）
- 配文遵循上方「文案核心原则」：反思总结式，每图一句核心要点，不逐图描述；带钩子 + 轻度道德绑架引导评论/点赞/关注
- `ending`：结尾互动文案

#### 2. 准备图片素材

自己搜图/做图，放到项目根目录：

- 封面：`cover-YYYYMMDD.jpg`
- 正文图：`img1.jpg`、`img2.jpg`、……、`imgN.jpg`（顺序与 `images` 数组一致）

**自动搜图（国内热点，WebFetch/WebSearch 搜不了时用）**——用 Bash curl 百度图片 acjson 接口，走本机网络绕开服务端策略：

```bash
UA="Mozilla/5.0 (iPhone; CPU iPhone OS 16_0) AppleWebKit/605.1.4 Mobile/15E148 Safari/604.1"
curl -s -A "$UA" -H "Referer: https://image.baidu.com/" \
  "https://image.baidu.com/search/acjson?tn=resultjson_com&word=关键词&pn=0&rn=20&ie=utf-8" -o /tmp/img.json
# python 读 data[].middleURL，逐张 curl 下载（带 Referer 防盗链），存到 zhangxue_imgs/ 等子目录
```

内容定主题同理抓 `https://m.baidu.com/s?word=关键词`（移动端 UA + Referer + zh-CN，反爬弱能拿真正文）。详见记忆 `cn-hotspot-via-curl`。

图片要求：

- 格式：JPG
- 单张建议 < 2MB（微信永久素材限制）
- 正文图最多 20 张

#### 3. 更新 Access Token

同上。

#### 4. 发布到草稿箱

```bash
cd /Users/zcy1/code_self/AI/project/dy-chat-img
node post_draft.js --draft "贴图草稿-YYYYMMDD.json"
```

#### 5. 记录发布结果

更新 `发布结果.md`。

---

## 贴图草稿 JSON 格式

```json
{
  "type": "wechat-image-post",
  "title": "借 1000 块钱，她回了 6 个好，结局把我看傻了",
  "summary": "从借钱到被反杀，全程高能。看到最后不笑算我输。",
  "cover": {
    "description": "微信聊天截图，借钱极限拉扯经典片段",
    "placeholder": "cover-20260709.jpg"
  },
  "images": [
    {
      "caption": "第一集：熟悉的开场",
      "text": "备注写着「初中—王刚（欠500）」。一开口就是借1000，还补一句「过两天给你」。女方只回了一个字：好。",
      "image_description": "微信聊天截图，男方借1000元，女方回复好"
    }
  ],
  "ending": "这操作我服了。被借过钱的都懂，评论区说说你遇到过什么样的借钱鬼才。",
  "style": "搞笑、扎心、日常",
  "color_tone": "白、绿"
}
```

---

## 关键参数说明

微信公众号图片消息 `draft/add` 接口与普通图文不同，必须注意：

| 字段 | 图文消息 `news` | 图片消息 `newspic` |
|---|---|---|
| `article_type` | `news`（默认） | 必须显式填 `newspic`，且放在 `articles[0]` 内 |
| 封面 | `thumb_media_id` | 也需要 `thumb_media_id` |
| 正文图片 | HTML `<img>` | 必须用 `image_info.image_list` 传永久素材 `media_id` |
| content | 支持 HTML | 只支持纯文本和部分特殊标签 |

错误示例（之前发成普通图文的原因）：

```json
{
  "articles": [{ "title": "..." }],
  "article_type": "newspic"
}
```

正确示例：

```json
{
  "articles": [
    {
      "article_type": "newspic",
      "title": "...",
      "content": "纯文本...",
      "thumb_media_id": "...",
      "image_info": {
        "image_list": [
          { "image_media_id": "..." },
          { "image_media_id": "..." }
        ]
      }
    }
  ]
}
```

---

## 常用命令

```bash
# 下载抖音链接原图
python3 download_douyin.py --url "https://v.douyin.com/xxxxx/" --out douyin_raw_imgs

# WebP 转 JPEG
mkdir -p douyin_raw_imgs_jpg
for f in douyin_raw_imgs/*.webp; do
  sips -s format jpeg "$f" --out "douyin_raw_imgs_jpg/$(basename "$f" .webp).jpg"
done

# 发布默认草稿
node post_draft.js

# 发布指定草稿 JSON
node post_draft.js --draft "贴图草稿-20250708.json"
```

---

## 注意事项

1. **access_token 会过期**：2 小时有效，发布前检查是否还有效。
2. **图片必须本地上传**：`post_draft.js` 会调用 `material/add_material` 把图片转成微信永久素材。
3. **抖音下载依赖 Playwright**：环境需安装 `playwright` 和 Chromium 浏览器。
4. **WebP 需要转换**：抖音原图常见 webp 格式，微信素材接口不认，必须先用 `sips` 转成 JPEG。
5. **并发问题**：同 AppID 多次获取 token 会互踩，同一批贴图串行发布，不要并发跑多个 `post_draft.js`。
6. **后台确认**：接口返回 `media_id` 后，去公众号后台「草稿箱」确认类型是否为「图片消息」。
7. **不做群发**：脚本只创建草稿，不调用群发接口。
8. **版权/隐私**：抖音图可能含他人头像/昵称/聊天记录等隐私，正式发布前建议打码或确认无侵权风险。
9. **图片保持原始比例**：直接用转码后的原图，不强制裁剪成统一比例，避免切掉正文内容。

---

## 不要做

- 不要把 `article_type` 放到 payload 顶层。
- 不要在 `newspic` 的 `content` 里塞复杂 HTML。
- 不要并发发布多篇贴图。
- 不要跳过后台确认类型。
- 不要把 webp 原图直接上传微信素材接口。
- 不要未经处理直接发布带他人头像/昵称的图。
- 不要逐图描述（「这张图是 XX」），文案要反思总结式、每图一句核心要点。
- 不要把图片强行裁成统一比例（3:4 等），用原图比例即可。
